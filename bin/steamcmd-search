#!/usr/bin/env node

var program = require('commander');

program
  .parse(process.argv);

var args = program.args;

if (!args.length) {
  console.error('query required');
  process.exit(1);
}

console.log('search : %s', args.join(' '));
